# Glossary

Project-specific vocabulary. Use these terms exactly. Agents and humans
both read this.

> When instantiating this template, start by copying the **Coder system**
> section from [`../system/glossary.md`](../system/glossary.md) (workers,
> roles, pipeline terms, artifact types) and then append a project-specific
> section below.

<!-- Cold-start ingestion note (spec 0045):
  The cold-start aggregator appends glossary entries in the format:

    **term** — definition. *(confidence: N/100)*

  Entries suffixed with "(alt)" are alternative definitions detected for the
  same term from different source paths. Reviewers must pick the better
  definition, delete the "(alt)" entry, and remove the confidence annotation
  before merging. Glossary entries are plain Markdown, not frontmatter, so
  ingestion_provenance does not appear inline — the provenance is recorded in
  the PR body instead.
-->

## Project terms

_Add your domain vocabulary here._
