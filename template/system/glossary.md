# Glossary

Project-specific vocabulary. Use these terms exactly. Agents and humans
both read this.

> When instantiating this template, start by copying the **Coder system**
> section from [`../system/glossary.md`](../system/glossary.md) (workers,
> roles, pipeline terms, artifact types) and then append a project-specific
> section below.

<!-- cold-start ingestion notes (spec 0045)
The cold-start aggregator appends glossary entries in the following structured
format — one fenced block per term at the bottom of the "Project terms" section:

  **term** — definition. *(source: path/to/file.go)*

Entries with an `(alt)` suffix were generated as alternative definitions for
the same term. Before merging the cold-start PR, pick the better definition,
delete the duplicate, and normalise the term to a single canonical form (no
`(alt)` suffix). See the cold-start review runbook for guidance.
-->

## Project terms

_Add your domain vocabulary here._
